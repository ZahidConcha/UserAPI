@page "/puestos"
@inject IPuestoRepo puestoRepo

<SfGrid DataSource="Model"> </SfGrid>

@code {
    private IList<Puestos> Model;

    protected override async Task OnInitializedAsync()
    {
        Model = await puestoRepo.GetAll(EndPoints.PuestosEndPoint);
    }
}
